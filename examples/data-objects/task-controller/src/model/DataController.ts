import { DataObject } from "@fluidframework/aqueduct";

export class Controller extends DataObject {
    protected async initializingFirstTime() {
        // const list = ListComponentInstantiationFactory.
    }

    protected async hasInitialized() {

    }
}